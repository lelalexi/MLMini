workspace 'MLMini'
project 'MLMini.xcodeproj'

use_frameworks!
platform :ios, '13.0'

target 'MLMini' do
  pod 'SDWebImage', '~> 5.0'

	target 'MLMiniTests' do
    		inherit! :search_paths
    	pod 'Quick'
    	pod 'Nimble'
  end
end
